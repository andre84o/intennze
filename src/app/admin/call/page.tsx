import CallCompanionClient from "./CallCompanionClient";

// Mobile Call Companion. Auth is enforced by the admin proxy + admin layout.
// Renders a minimal, full-screen, phone-first call view.
export default function CallPage() {
  return <CallCompanionClient />;
}
