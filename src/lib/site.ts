/**
 * Canonical public site URL used by indexable SEO surfaces such as sitemap and
 * robots. Keep this fixed to the preferred production host so preview or
 * misconfigured environment URLs can never leak into canonical crawl signals.
 */
export const SITE_URL = "https://www.intenzze.com";
